$schema: 'https://moonrepo.dev/schemas/workspace.json'

projects:
  globs:
    - './apps/*'
    - './packages/*/*'

vcs:
  provider: 'github'
  defaultBranch: 'main'

notifier:
  terminalNotifications: 'always'
experiments:
  fasterGlobWalk: true
  gitV2: true
